@AGENTS.md

# Token saving rules — PHẢI TUÂN THỦ

- KHÔNG dùng Agent tool trừ khi thật sự cần (search > 5 file hoặc task song song độc lập)
- Dùng Grep, Glob, Read trực tiếp thay vì spawn agent để search
- KHÔNG spawn nhiều agent cùng lúc khi có thể làm tuần tự
- Trả lời ngắn gọn, đi thẳng vào vấn đề
- KHÔNG giải thích dài dòng trừ khi user hỏi
- KHÔNG đọc lại file vừa edit (Edit tool đã xác nhận thành công)
- KHÔNG chạy agent để "verify" hoặc "double check" — tin kết quả tool
- Ưu tiên sửa 1 lần đúng, không sửa rồi đọc lại rồi sửa lại
- KHÔNG tự ý sửa code ngoài yêu cầu của user
